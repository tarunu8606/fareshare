// Placeholder — Individual ride detail page (dynamic route)
// [id] means the URL can be /rides/123, /rides/abc, etc.
export default function RideDetailPage() {
  return <div>Ride Detail Page</div>
}
